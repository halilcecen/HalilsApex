public class ListAssigment {
    public static void Practice(){ 

        List<integer> MyLimits = new list<integer>();
    for(integer i=1; i<=12; i++){
   If(math.mod(i,2)==0);
    MyList.add(i);
}
    system.debug(MyLimits);
    
    
    }
    
    }
