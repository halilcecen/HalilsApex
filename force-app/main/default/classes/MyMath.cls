public class MyMath {
    public integer add(integer a, integer b){
       system.debug(a+b);
       return(a+b);
    }
    public void add(integer a, integer b, integer c){
system.debug(a+b+c);
    }
    public void add(integer a, integer b, integer c, integer d){
        system.debug(a+b+c+d);
}
}